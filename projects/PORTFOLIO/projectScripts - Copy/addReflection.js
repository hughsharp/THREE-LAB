import * as THREE from 'three';

/**
 * Manages the "disappear" and "reappear" animations for a set of objects.
 * This class creates and starts tweens but does not handle the global update loop.
 */
export const reflector