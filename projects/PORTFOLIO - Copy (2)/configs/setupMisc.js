// import * as THREE from 'three'; // Assuming you have Three.js installed and can import it

// export class Misc {
//     constructor(model, options = {}) {
//         this.options = options;
//         this.clock = options.clock || new THREE.Clock();

//     }
    
//     update() {
//         if (this.options.clock instanceof THREE.Clock) {
//             const delta = this.clock.getDelta() + 0.00001
//         }
//         if (this.options.mixer )
//     }

    
// }